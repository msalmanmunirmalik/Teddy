[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback - redirect all routes to index.html
# This works in conjunction with the _redirects file in public/
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "20"

